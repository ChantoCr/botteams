# Sample queue Excel schema

Suggested columns for the `QueueTable`:

- `displayName` (string)
- `aadObjectId` (string)
- `status` (available|busy|break|lunch|offline)
- `queueOrder` (integer)
- `boostMode` (normal|double)
- `lastUpdatedUtc` (ISO datetime)
